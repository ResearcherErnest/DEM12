# Default: normalise line endings to LF on commit
* text=auto eol=lf

# Shell scripts and SQL must always stay LF
*.sh   text eol=lf
*.sql  text eol=lf

# Windows-only files keep CRLF
*.bat  text eol=crlf
*.cmd  text eol=crlf
*.ps1  text eol=crlf
